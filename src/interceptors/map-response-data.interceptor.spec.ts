import { MapResponseDataInterceptor } from './map-response-data.interceptor';

describe('MapResponseDataInterceptor', () => {
  it('should be defined', () => {
    expect(new MapResponseDataInterceptor()).toBeDefined();
  });
});
